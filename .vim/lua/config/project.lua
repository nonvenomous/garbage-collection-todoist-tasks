local lspconfig = require("lspconfig")

lspconfig.vtsls.setup({ enabled = false })
lspconfig.eslint.setup({ enabled = false })
lspconfig.bashls.setup({ enabled = false })
lspconfig.svelte.setup({ enabled = false })
lspconfig.tailwindcss.setup({ enabled = false })
